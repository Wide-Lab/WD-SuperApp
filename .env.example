# Credenciais do Postgres usadas pelo docker-compose (serviço `db`).
# Copie para `.env` e ajuste se quiser valores diferentes dos defaults abaixo.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=central
